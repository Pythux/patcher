from setuptools import setup

setup(
    name='patcher',
    version='1.0',
    description='save/load a directory with patchs and previous saves (bsdiff4)',
    author='Pythux',
    # author_email='',
    packages=['patcher'],  # same as name
    # install_requires=[],  # external packages as dependencies
)
